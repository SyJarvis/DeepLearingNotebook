# 线性回归（LinearRegression）

在此不会记录详细的知识点，而是只记录关键词，详细的资料会在文后附上链接。

术语：





线性回归

模型



数据集(data set)

​	训练数据集(training data set)or测试数据集(test data set)

​	标签(label)

​	样本(sample)

​	特征(feature)



损失函数(loss function)

优化函数-随机梯度下降------（梯度下降法、梯度上升法）

​	解析解（analytical solution）

​	数值解（numerical solution）

